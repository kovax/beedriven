FeatureModel {
    ProductRef( name: "My First WebShop", version: "0.1" ) {
        optional {
            FeatureRef( name: "Customer Self Management" ) {
                mandatory {
                    FeatureRef( name: "Address Management" )
                    FeatureRef( name: "Shipping Management" )
                }
            }
        }

        mandatory {
            FeatureRef( name: "Security" ) {
                mandatory {
                    FeatureRef(name: "Customer Authentication") {
                        scenarios {
                            ScenarioRef(name: "Login")
                            ScenarioRef(name: "Logout")
                            ScenarioRef(name: "Change Password") {
                                implemented = true
                                automated = false
                                executed  = false
                                failed = false
                            }
                            ScenarioRef(name: "Forgotten password") {
                                implemented = true
                                automated = false
                                executed  = false
                                failed = false
                            }
                        }
                    }
                }
            }
        }
    }
}

