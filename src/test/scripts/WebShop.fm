define {
    fmFeature( name: "My First WebShop", version: "0.1" ) {
        optional {
            fmFeature( name: "Customer Self Management" )
        }

        mandatory {
            fmFeature( name: "Security" ) {
                mandatory {
                    fmFeature(name: "Customer Authentication")
                }
            }
        }
    }
}

