scenario "Error while changing Password", {
    given "logged in Customer"
    and  "Customer is on the Change password page"
    when "Customer enters incorrect values into the fields"
    then "error message describing the error should be shown"
}
