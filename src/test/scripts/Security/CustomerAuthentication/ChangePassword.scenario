scenario "Change Password", {
    given "Logged in Customer"
    and  "Customer is on the Change password page"	
    when "Customer fills every field correctly"
    then "Password is changed by the system"
    and "the Customer should be able to login with the new password"
}
